// У нас є пустий DOM-елемент elem та рядок text.
// Які з цих трьох команд виконають одну й ту ж дію?

elem.append(document.createTextNode(text))
elem.innerHTML = text
elem.textContent = text

// -------------------------------------------------

// усі три виконують одну і туж дію якщо вставляється лише один текст 
// якщо там будуть теги тоді 1 і 3 будуть давати однаковий результат але з вписаними тегами 